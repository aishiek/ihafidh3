import { Verse } from '@/types';

// This file is intentionally left empty to avoid conflicts with the database approach.
// All Quran data is now fetched from the API and stored in SQLite.
// This prevents TypeScript errors from duplicate exports.

// For essential verses that might be needed for offline fallback,
// use the database/QuranDatabase.ts file and the API services.